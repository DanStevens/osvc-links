{
  "name": "osvc-links",
  "version": "1.2.4",
  "private": true,
  "homepage": "http://danstevens.github.io/osvc-links",
  "author": "Dan Stevens <dan.stevens@oracle.com>",
  "dependencies": {
    "is-url": "^1.2.4",
    "react": "^16.5.2",
    "react-dom": "^16.5.2",
    "react-scripts": "2.0.4"
  },
  "scripts": {
    "start": "react-scripts start",
    "build": "react-scripts build",
    "test": "react-scripts test",
    "eject": "react-scripts eject",
    "predeploy": "npm run build",
    "deploy": "gh-pages -d build -r https://github.com/DanStevens/osvc-links.git",
    "gh-pages-clean": "gh-pages-clean",
    "gh-pages-debug": "node --version && set NODE_DEBUG=net,gh-pages && echo %NODE_DEBUG%"
  },
  "eslintConfig": {
    "extends": "react-app"
  },
  "browserslist": [
    ">0.2%",
    "not dead",
    "not ie <= 11",
    "not op_mini all"
  ],
  "devDependencies": {
    "gh-pages": "^2.0.1"
  }
}
